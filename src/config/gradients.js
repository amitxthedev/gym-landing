// src/config/gradients.js
// Modern 3D + premium gradients (gym / fitness / luxury)

const gradients = {
  /* =====================
     HERO / MAIN
  ===================== */
  heroPrimary: "linear-gradient(135deg, #00E5FF 0%, #7C4DFF 100%)",
  heroDark:
    "radial-gradient(1200px circle at top, rgba(124,77,255,0.25), #050505)",

  /* =====================
     CARDS (GLASS / 3D)
  ===================== */
  cardSoft:
    "linear-gradient(135deg, rgba(255,255,255,0.12), rgba(255,255,255,0.04))",

  cardGlow:
    "linear-gradient(135deg, rgba(0,229,255,0.25), rgba(124,77,255,0.25))",

  /* =====================
     BUTTONS / CTA
  ===================== */
  buttonPrimary:
    "linear-gradient(135deg, #00E5FF 0%, #7C4DFF 60%)",

  buttonAccent:
    "linear-gradient(135deg, #FF8F00 0%, #FF6D00 100%)",

  /* =====================
     SECTION BACKGROUNDS
  ===================== */
  sectionLight:
    "linear-gradient(180deg, #FFFFFF 0%, #F6F7FB 100%)",

  sectionDark:
    "linear-gradient(180deg, #050505 0%, #0B0B10 100%)",

  /* =====================
     OVERLAYS / BLURS
  ===================== */
  blurBlue:
    "radial-gradient(600px circle, rgba(0,229,255,0.35), transparent 70%)",

  blurPurple:
    "radial-gradient(600px circle, rgba(124,77,255,0.35), transparent 70%)"
};

export default gradients;
